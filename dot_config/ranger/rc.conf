map i shell if [ $(tmux list-panes | wc -l) -gt 1 ]; then tmux send-keys -t :.1 "clear && kitty +kitten icat \"$(realpath %p)\"" Enter; else tmux split-window -h "kitty +kitten icat \"$(realpath %p)\" && cat"; fi